import React from 'react'

const Title = ({ title }) => {
    return <span className="text-sm font-light tracking-tight text-gray-800 uppercase dark:text-gray-500">{title}</span>
}

export default Title
